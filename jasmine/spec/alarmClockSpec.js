describe("Alarm Clock", () => {
    describe("Build the Clock", () => {
        describe("Variable name context", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
    });
    describe("Build the Alarm", () => {
        it("Write you test expectation here", () => {
            // arrange

            // act

            // assert
            expect().toBe();
        });
    });
});
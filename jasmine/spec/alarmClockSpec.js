describe("Alarm Clock", () => {
    describe("Build the Clock", () => {
        describe("Configure Global Variables", () => {
            it("Variable name canvas to be defined", () => {
                expect(clockImage).toBeDefined();
                expect(canvas).toBeInstanceOf(HTMLElement);
            });
            it("Variable name canvas to be passed the HTML Canvas DOM element ID", () => {
                expect(canvas).toContain('clockCanvas');
            });
            it("Variable name canvas to be defined", () => {
                expect(context).toBeDefined();
                expect(clockImage).toBeInstanceOf(HTMLCanvasElement);
            });
            it("Variable name canvas to be defined", () => {
                expect(clockImage).toBeDefined();
                expect(clockImage).toBeInstanceOf(HTMLImageElement);
            });
            it("Variable name clockImageLoaded to be defined", () => {
                expect(clockImageLoaded).toBeDefined();
                expect(clockImageLoaded).toBeInstanceOf(Boolean);
            });
            it("Variable name clockImageLoaded value", () => {
                expect(clockImageLoaded).toBe(true);
            });
        });
        describe("Variable name context", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
        describe("Build the Clock", () => {
            it("Write you test expectation here", () => {
                // arrange

                // act

                // assert
                expect().toBe();
            });
        });
    });
    describe("Build the Alarm", () => {
        it("Write you test expectation here", () => {
            // arrange

            // act

            // assert
            expect().toBe();
        });
    });
});